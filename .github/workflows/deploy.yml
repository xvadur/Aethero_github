# Placeholder for ./.github/workflows/deploy.yml
# Generated by autofix_engine.py on 2025-06-05T22:57:28.499816

# deploy.yml workflow je dočasne deaktivovaný, aby nespôsoboval chyby v Actions.
# Ak chcete workflow aktivovať, doplňte sem reálnu definíciu.

name: deploy (DISABLED)

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy workflow is currently disabled
        run: echo "This workflow is intentionally disabled."
